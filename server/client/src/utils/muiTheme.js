import { createTheme } from "@mui/material";

export const muiTheme = createTheme({
  typography: {
    fontSize: 11,
  },
});

export const muiUploader = createTheme({
  typography: {
    fontSize: 40,
  },
});